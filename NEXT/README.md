# NEXT changelog fragments

Add one `YYYY-MM-DD-issue-N-slug.md` file per behavior, documentation, dependency, or
configuration change. Each fragment contains `date`, `issue`, `impact`, and `title`
front matter followed by a concise release note.
